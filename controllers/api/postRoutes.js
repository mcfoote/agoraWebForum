const router = require('express').Router();
const {Post} = require('../../models');

router.post('/boards/:name/:thread_id/:post_id', (req, res) => {

});

router.delete('/boards/:name/:thread_id/:post_id', (req, res) => {

});

module.exports = router;